# CT 결함 모델 확인 요청 — 회신

> 요청: `CT_결함모델_확인요청사항.md` (2026-08-07)
> 회신: 2026-08-07 · 관련 커밋 `cc8bf68 모델 카드 1280 수정`

---

## Q1. 배포 입력 크기 — **`1280` 확정입니다**

**`640` 은 카드의 오기였습니다. 카드를 고쳤습니다.**

확인해 주신 내용이 정확합니다. 저희도 독립적으로 소스에서 같은 결론에 도달했고, 두 근거가 맞물립니다.

| | 근거 |
| --- | --- |
| ① 체크포인트 | `ct_samedist_CHAMPION_ep6.pt` → `train_args.imgsz = 1280` |
| ② ultralytics 8.4.115 | `engine/model.py::_reset_ckpt_args` 의 `include = {"imgsz", "data", "task", "single_cls"}` — **`imgsz` 를 체크포인트에서 복원해 `model.overrides` 에 넣습니다** |
| ③ `predict()` | `args = {**self.overrides, **custom, **kwargs}`, `custom` 에 `imgsz` 없음 |
| ④ sahi 0.12.5 | `image_size` 가 `None` 이면 `kwargs` 에 `imgsz` 를 안 넣음 |
| ⑤ **런타임 (귀사 확인)** | `image_size` 미지정 → 골든 픽스처 정확 재현 / `imgsz=640` 강제 → **검출 0건** |

⑤ 가 결정적입니다. **골든 픽스처는 배포 경로에서 만든 스냅샷**이므로, 640 이 정본이었다면
640 에서 재현돼야 합니다. 정반대가 나왔다는 것이 곧 배포 경로가 1280 이라는 증명입니다.

### 재산출 필요 없습니다

confidence 운영점(`0.05`)·성능 지표(R 0.977)·골든 픽스처는 **전부 실제 배포 경로에서 측정된 값**입니다.
바뀐 것은 그 경로를 부르는 이름뿐입니다. **재산출하지 마세요.**

### 왜 640 이라고 적혀 있었나 (같은 실수 방지용)

진단 코드가 `ultralytics.cfg.get_cfg()` 를 읽었습니다. 그건 **글로벌 `DEFAULT_CFG`** 이지
이 모델이 쓰는 값이 아닙니다.

- `iou 0.7` · `max_det 300` → `include` 집합에 **없어서** 그 방법이 **맞습니다**
- `imgsz` → 체크포인트에서 **복원되는 4개 키 중 하나**라 그 방법이 안 통합니다

`fixtures/env.json` 에 `ultralytics_default_imgsz` 키가 아예 없다는 것도 방증입니다 —
**한 번도 측정된 적 없는 값이었습니다.** 진단 코드를 모델에서 직접 읽도록 고쳤고,
`SLICE` 와 다르면 `assert` 로 멈춥니다.

### 딸려서 폐기된 것

카드 §9 의 "`image_size=1280` 을 켜면 recall 이 오를 수 있다" 개선안을 **삭제**했습니다.
이미 1280 이라 아무것도 안 바뀝니다.

---

## Q2. ONNX bbox 통과 기준

### 결론 먼저

| | |
| --- | --- |
| **`±1px` 유지해야 하나** | **아니오.** 백엔드가 바뀌면 맞지 않는 기준입니다 |
| **그럼 6px 로 넓히면 되나** | **아니오.** CT 결함은 폭 5px 라 6px 는 완전 이탈입니다 |
| **그럼 뭘로 하나** | **판정 기준을 bbox 에서 걷어내세요.** 아래 §2.3 |
| **지금 ONNX 를 써도 되나** | **판단 보류.** n=1 로는 못 정합니다 — 픽스처 20장 후 결정 (§2.4) |

### 2.1 `±1px` 은 PT↔PT 용이었습니다

`±1px` 은 **같은 백엔드를 다시 돌렸을 때** 결과가 흔들리지 않는지 보는 값입니다.
ONNX 는 연산 그래프가 다른 백엔드이므로 오차의 **성격**이 다릅니다. 같은 자로 재면 안 됩니다.

### 2.2 그런데 `6px` 도 그냥 받으면 안 됩니다 — CT 결함이 얇습니다

현재 골든 픽스처의 유일한 검출이 **5 × 292 px** 입니다. 세로로 긴 슬리버입니다.
같은 "6px" 이 축에 따라 이렇게 다릅니다:

| 이동 | IoU |
| --- | --- |
| x축 +1px | **0.667** |
| x축 +2px | 0.429 |
| **x축 +6px** | **0.000** ← 박스가 결함을 아예 안 덮습니다 |
| y축 +6px | **0.960** |
| y축 +20px | 0.872 |

**"최대 6px" 하나로는 통과인지 완전 이탈인지 구분이 안 됩니다.**
그리고 IoU 로 바꿔도 해결이 안 됩니다 — 폭 5px 에서는 **x축 1px 만 움직여도 IoU 0.667** 이라
IoU 임계를 걸면 PT↔PT 조차 통과 못 합니다. **스칼라 기하 기준 자체가 이 모양에 안 맞습니다.**

### 2.3 제안 — 판정은 게이트로, 기하는 경고로

CT 모델이 제품에 파는 것은 **셀의 PASS/REJECT 판정**입니다. bbox 는 리포트 오버레이용 파생물이고,
**게이트 판정에는 bbox 가 쓰이지 않습니다**(검출 유무만 씁니다).

| 층 | 항목 | 기준 | 현재 ONNX |
| --- | --- | --- | --- |
| **실패** | `verdict` (PASS/REJECT) | **완전 일치** | ✅ 일치 |
| **실패** | 검출 수 | **완전 일치** | ✅ 일치 |
| **실패** | `class` | **완전 일치** | ✅ 일치 |
| **실패** | `confidence` | **±0.01** | ✅ 0.00346 |
| 경고 | bbox 중심 거리 · 변 길이비 | 기록만 | ⚠️ 최대 6px |
| 경고 | mask 면적비 | ±5% | 미측정 |

**실패 4개는 이미 전부 통과했습니다.** 이 구조라면 ONNX 는 현재 상태로 합격입니다.

같은 구조를 외관(EXT) 모듈에도 이미 적용했습니다 — 게이트 동작만 실패로 두고 좌표는 경고입니다.
**경고까지 실패로 만들면 픽스처가 매번 깨지고, 매번 깨지는 테스트는 아무도 안 봅니다.**

### 2.4 다만 n=1 로 정하지 마세요 — 6px 은 연속값이 아닙니다

**왜 직접 추론은 1px 인데 SAHI 를 거치면 6px 이 되는지**가 중요합니다.
누적 반올림이 아닙니다. 저희 병합 설정은 `postprocess_type='NMS'` / `match_metric='IOU'` (카드 §4)
이고, **NMS 는 겹치는 박스 중 하나를 고르고 나머지를 버립니다.**

즉 인접 슬라이스가 같은 결함을 각자 잡았을 때, confidence 가 `0.003` 만 달라져도
**어느 슬라이스의 박스가 살아남는지가 뒤바뀝니다.** 슬라이스가 다르면 박스는 원래 몇 px 씩 다르므로,
바뀌는 순간 그 차이가 통째로 나타납니다.

> **이건 이산(discrete) 현상입니다.** ONNX 수치를 더 정밀하게 만들어도 안 줄고,
> 다른 이미지에서는 6px 이 아니라 **수십 px** 이 나올 수도 있습니다. 반대로 대부분은 0px 입니다.
> **1장에서 잰 6px 으로 임계를 정하면 안 됩니다.**

→ 아래 픽스처 20장을 먼저 돌려 **편차 분포**(0px 몇 장 / 몇 px 몇 장)를 보고 확정하시죠.
회귀 셀이 그 분포를 바로 출력하도록 만들어 두었습니다.

### 2.5 그때까지

**PT 서빙으로 진행하셔도 되고, 위 4개 실패 기준만으로 ONNX 를 채택하셔도 됩니다.**
저희 권고는 **후자**입니다 — 게이트 동작이 동일하다는 것은 이미 입증됐고,
bbox 는 리포트 표시용이라 몇 px 차이가 제품 판단을 바꾸지 않습니다.
다만 **채택한다면 위 표를 계약으로 박아 주세요.** "±1px 을 못 지켰지만 넘어갔다" 로 남으면
다음 사람이 되돌립니다.

---

## 골든 픽스처 20장

`battery_infer_to_json.ipynb` 에 **§7 (생성) · §8 (회귀)** 셀을 추가했습니다.
`§1 → §2 → §7` 순으로 돌리면 `fixtures/golden_set.json` + `fixtures/images/` 20장이 나옵니다.

| | |
| --- | --- |
| 구성 | **REJECT + PASS 를 섞습니다** — 결함만 있으면 "전부 결함으로 만드는" 회귀를 못 잡습니다 |
| 축 | x · y · z 를 고르게 — 축마다 ROI 크기가 달라서(카드 §3) 축 편중은 위험합니다 |
| 선정 | REJECT 는 **임계 여유가 큰** 순 — 임계에 붙은 케이스는 GPU 만 바뀌어도 뒤집힙니다 |
| 기록 | 케이스마다 `margin`(conf − 임계)을 같이 굳혀서, 깨졌을 때 "원래 아슬아슬했나"를 구분합니다 |
| 기존 `golden_ct.*` | **그대로 둡니다** — 이미 쓰고 계신 회귀가 안 깨지게 |

§8 은 위 §2.3 구조 그대로 **실패/경고를 나눠서** 판정하고, 마지막에
**bbox 편차 분포**를 출력합니다. 그게 Q2 의 임계를 정할 데이터입니다.

---

## 요약

| | |
| --- | --- |
| Q1 | **1280 확정.** 카드 수정 완료(`cc8bf68`). **재산출 불필요** |
| Q2 | `±1px` 폐기 · 6px 채택도 아님 → **게이트 4항목 실패 / 기하 경고** 구조 제안. 임계 확정은 픽스처 20장 후 |
| 픽스처 | §7·§8 셀 추가. 실행은 GPU 필요 |
